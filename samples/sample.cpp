#include <duifene_sign.h>
#include <iostream>

int main() {
    // duifene_sign::Session session;
    // std::string user_link;
    // std::cout << "请输入登录链接: ";
    // std::cin >> user_link;
    // session.login(user_link);
    // session.print_course_list();
    // int idx;
    // while (std::cin >> idx) {
    //     duifene_sign::SignInfo sign_info = session.get_sign_info(idx);
    //     if (sign_info.hf_checktype != "0") {
    //         session.sign(sign_info);
    //     }
    //     sign_info.print();
    // }

    std::cout << "hello world\n";

    return 0;
}